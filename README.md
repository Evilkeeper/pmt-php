#Тестовое задание на знание PHP

`task.php` содержит функцию с заданием

`test.php` можно запустить из консоли. Забирает массив из `in.php`, пропускает через функцию и выводит `var_export` результата в `out.txt`